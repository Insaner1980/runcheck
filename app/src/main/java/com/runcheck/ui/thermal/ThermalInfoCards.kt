package com.runcheck.ui.thermal

/**
 * Info card IDs for the Thermal detail screen.
 *
 * Card IDs include a version suffix (e.g. "_v1"). When card content is updated
 * in a future release, bump the version so that previously dismissed cards
 * resurface with the new content.
 */
object ThermalInfoCards {
    const val THROTTLING_EXPLAINER = "thermal_throttling_explainer_v1"
    const val HEAT_BATTERY_LOOP = "thermal_heat_battery_loop_v1"
}

package com.runcheck

import android.content.Context

object SentryInit {
    @Suppress("UNUSED_PARAMETER")
    fun init(context: Context) {
        // No-op: Sentry is not included in release builds
    }
}

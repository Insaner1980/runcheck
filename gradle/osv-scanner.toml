# Keep this file suppression-free by default. Add only exact, time-bounded
# [[IgnoredVulns]] entries after reviewing a concrete advisory; do not use
# PackageOverrides to hide every current and future advisory for a package.

[[IgnoredVulns]]
id = "GHSA-r937-wjx7-w2jp"
ignoreUntil = 2026-09-30
reason = "Kotlin 2.4.10 is the latest stable toolchain; the fixed 2.4.20 line is still pre-release. Gradle and Kotlin build caches are disabled through org.gradle.caching=false and kotlin.caching.enabled=false, so the vulnerable deserialization path is not enabled. Revisit when a stable fixed Kotlin release is available."

# runcheck Changelog

_Auto-generated from git history. Updated on each commit._

## 2026 Week 13 (Mar 23 – Mar 29)

### Fixed
- Fix dependency-analysis plugin version (`cdb9154`)
### Other
- Harden release logging and latency config (`a324e77`)
- CHANGELOG-päivitys: viimeisin commit lisätty automaattisesti (`64d06cf`)
- Laaja koodilaadun refaktorointi: annotaatiot, nimeäminen, arkkitehtuuri ja kuolleen koodin poisto (`ddabc45`)
- Log.w → ReleaseSafeLog.warn: projektin lokituskäytännön mukainen (`1effd22`)
- SonarCloud-koodihajujen korjaus: kognitiivinen kompleksisuus, tyhjät catch-lohkot, if→when (`4abac22`)
- trigger sonar (`b9d31ac`)
- GridCard subtitleColor-bugikorjaus + SonarCloud-analyysi dokumentoitu (`24f56a1`)
- SonarCloud-koodihajujen korjaus: käyttämättömät muuttujat, duplikaattiliteraalit ja -haarat (`033c98d`)
- trigger sonar analysis after fixes (`4afb11c`)
### Added
- Add tools:replace for android:usesCleartextTraffic (`50e5c97`)
- trigger build (`4186d99`)
- Lyhennetty FGS-syyteksti yksinkertaisemmaksi (`9fff055`)
- Silent Observer UI-redesign: typografia-dominantti hero-näkymät ja tonaalinen kerrostus (`f16db7a`)
- Update changelog after push commit (`4883c4d`)
- Commit latest runcheck changes (`cfb12b6`)
- SDK 37 preview, Detekt 1.23.8 + Compose Rules, annotaatiot ja koodilaadun parannukset (`4ee4402`)
- Move hardcoded separators and concatenated text to string resources (`dfab75f`)
- Add defense-in-depth pro checks to network history and storage cleanup (`d2ca2e4`)
- Add Security & Quality Requirements section to CLAUDE.md (`246853b`)
- Fix CodeQL implicit PendingIntent findings in notifications (`841068f`)
- Fix two compilation errors: saveResult return type and variable ordering (`4becd48`)
- Update CodeQL workflow: v3→v4, add build-mode: manual (`906433c`)
- Add custom CodeQL workflow for Android/Kotlin project (`1522b5d`)
- Remove MANAGE_EXTERNAL_STORAGE permission and dead ScreenStateReceiver intent-filters (`b6df3c4`)
- Remove redundant package_name index and fix deprecated fallbackToDestructiveMigration (`29f0391`)
- Add detekt Compose rules plugin and document PagingData exception (`5e44cfc`)
- Configure static analysis: add Android Lint block and expand detekt rules (`3bbbeec`)
- Fix outdated counts in CLAUDE.md and UI-SPEC.md (`6132566`)
- feat: laaja päivitys — speed test, chart-animaatiot, UI-parannukset, palvelut (`b1290a7`)
### Docs
- docs: päivitä CODEX.md ja AGENTS.md — poista crash reporting -viitteet (`aa6ef71`)
- docs: päivitä PROJECT.md ja UI-SPEC.md vastaamaan nykytilaa (`1c53b01`)
### Changed
- refactor: poista Firebase Crashlytics kokonaan (`242df67`)
- feat: varmistusdialogit kaikille tuhoaville toiminnoille (`f100915`)
- feat: speed test yhteystyyppi, latenssi-mittaus ja jitter-korjaus (`e9ee1d8`)
- feat: edellisten sessioiden muutokset — lokalisaatio, cleanup, info-sisältö (`52137ec`)
- docs: päivitä PROJECT.md ja UI-SPEC.md chart-animaatioilla (`7e56e0e`)
- docs: päivitä CLAUDE.md ja ui-reference.md chart-animaatiomuutoksilla (`e8ca875`)
- fix: lisää puuttuva clipRect import ja HistoryPeriod-haarat (`abeb59a`)
- feat: integroi chart-animaatiot kaikkiin detail-näkymiin (`433dbbc`)
- feat: LiveChart pehmeä liuku ja glow-pulssi uusille datapisteille (`cc9f57d`)
- feat: AreaChart oskilloskooppi-pyyhkäisy ja parannettu täyttö (`3771997`)
- feat: TrendChart datanvaihtosiirtymä (fade-out + uusi pyyhkäisy) (`f592729`)
- feat: TrendChart viimeisen arvon korostus (glow-piste + katkoviiva) (`2873986`)
- feat: TrendChart parannettu gradient-täyttö datan korkeuden mukaan (`103d2c1`)
- feat: TrendChart status gradient -viiva quality zone -väreillä (`1aff813`)
- feat: TrendChart oskilloskooppi-pyyhkäisy ja grid-first sisääntulo (`6e84e30`)
- feat: lisää storageQualityZones ja qualityZoneColorForValue-apufunktio (`aa8097a`)
- docs: lisää chart-animaatioiden toteutussuunnitelma (9 taskia) (`710c668`)
- docs: lisää chart-animaatioiden design spec ("Instrument Sweep") (`bc36d27`)
- feat: add storage usage history TrendChart to storage detail screen (`46ee446`)
- feat: add temperature history TrendChart to thermal detail screen (`7abf493`)
- feat: add live signal strength chart on network detail screen (`33dd7f0`)
- feat: add live charts for battery level, temperature, and voltage (`5231e7d`)

## 2026 Week 12 (Mar 16 – Mar 22)

### Fixed
- Fix notification tap actions, deep-linking, and worker locale (`3eb095e`)
- Fix live notification service not surviving device reboot or process death (`5fd0dec`)
- Fix charge-complete oscillation and alert retry consistency bugs (`a2f0036`)
### Changed
- Rename notification channel IDs from device_pulse to runcheck (`ee7a3d3`)
- Fix notification channel, permission, and ID issues (`d4362e8`)
### Added
- Lisää live-notifikaatio, kielivalinta, uusi sovellusikoni ja dokumentaatio (`ae9178d`)
- Korjaa Storage-kortin gauge-väri ja MetricPill-tasaus (`c2cbd10`)
### UI
- UI-viilaukset: dialogien muodot, CrossLinkButton-layout, locale-config, MediaStore-kyselyt (`ce9bd3c`)
- Add fullscreen charts and educational content updates (`252a884`)
- Fix educational content: update Wi-Fi 7 references and add cross-links from info cards to Learn articles (`2c1eb2e`)
- Add contextual Learn article links to detail screens (`3a8bc08`)
### Other
- Simplify Learn section topic management and fix localization (`f53dede`)
- Fix info card display conditions, add dismiss animation, and reset tips (`9b1cd14`)
- Add version suffixes to info card IDs for content update resurfacing (`3160a2d`)
- Fix fullscreen chart empty state to show period/metric filter chips (`fe8ee4c`)
- Fix fullscreen chart content hidden behind system bars and cutouts (`02e6d5f`)
- Fix fullscreen chart loading flicker, selection sync-back, and session tooltip format (`cc5b27d`)
- Fix fullscreen chart orientation restore and add process death test (`55a918d`)
- Fix fullscreen chart retry button corrupting period selection (`76512c4`)
- Fix edge cases: DB corruption, speed test lifecycle, state persistence, error handling (`ba38cee`)
- Fix Glance widget issues: click action, responsiveness, previews, partial data (`5c23685`)
### Docs
- Päivitä CLAUDE.md vastaamaan nykyistä koodikantaa (`b750916`)
- Implement latest runcheck app updates (`12075b1`)
- Add Quick Actions card to Storage Detail Screen (`dbd1d6d`)
- Add Normal and Critical temperature bands to thermal detail screen (`be80bb4`)
- Add missing speed test screen states: Start Test, progress %, server, Run Again (`8b6c4cf`)
- Refine network detail screen: separate IP/DNS card, gate signal history behind Pro (`e4902ee`)
- Add animateItem for group expand/collapse and fix delete cancel behavior (`17e104e`)
- Laajat UI-parannukset: graafit, storage cleanup, HeatStrip, Pro debug (`17d3779`)
- Apply pending runcheck updates (`de3159c`)
- Päivitä CLAUDE.md arkkitehtuurimuutosten mukaiseksi (`62eb703`)
- Arkkitehtuurikorjaukset: Clean Architecture, DI, siivous (`92f8d77`)
- fix: remove forbidden !! operator from ProUpgradeScreen and FileListItem (`aa55ed1`)
- fix: add missing error handling in thermal zone discovery and cleanup delete (`62e269e`)
- fix: add @Volatile to SpeedTestService.activeTest for thread visibility (`831b7f7`)
- fix: prevent resource leaks in battery source, ProManager, and thumbnails (`d8a4abd`)
- fix: remove 41 unused string resources from both locales (`39c578d`)
- fix: remove additional dead code (composables, use cases, DAO methods) (`7c935dd`)
- fix: remove dead code and wire up billing cleanup (`9787fe5`)
- Fix reduced motion and API level guard violations (`e5cbd1f`)
- fix(battery): correct health score weights and battery source factory priority (`82fe5cc`)
- Fix battery module: API 31 guard, statistics query efficiency, code style (`33f1d06`)
- Fix battery module: API 31 guard on RenderEffect, remove !! operator (`db106e2`)
- Fix codebase review issues: layer violations, API guards, Pro gating, reduced motion, design (`7a64e20`)
- Add files via upload (`392645a`)
- Laaja ominaisuuspäivitys: akku, storage, thermal, asetukset (`8980fc3`)

## 2026 Week 11 (Mar 09 – Mar 15)

### Changed
- Uudista NetworkDetailScreen korttipohj. layoutiin (`d36ac53`)
### Added
- Lisää signaalihistoria NetworkUiState- ja ViewModel-kerroksiin (`bac70f0`)
- Lisää statusColorForSignalQuality, SignalBars-komponentti ja verkkonäkymän merkkijonot (`cdda643`)
- Lisää GetNetworkHistoryUseCase ja getReadingsSince repositoryyn (`5a60588`)
- Lisää uudet verkkokentät NetworkRepositoryImpl-mappaukseen (`87e2288`)
### Other
- Laajenna NetworkDataSource keräämään lisäverkkotietoja (`138c86d`)
- Laajenna NetworkState-domainmalli verkkotiedoilla (`daf6181`)
- Lisää network detail screen -uudistuksen toteutussuunnitelma (`409b2ba`)
- Lisää network detail screen -uudistuksen suunnitteludokumentti (`878bb43`)
- Uudelleennimeä paketti devicepulse → runcheck ja lisää UI-parannukset (`25fd2fd`)
- Lisää Pro UI premium-uudistuksen suunnitteludokumentti (`e08f5aa`)
- Päivitä home screen polish -speksi review-palautteen perusteella (`5ea060c`)
- Lisää home screen visual polish -suunnitteludokumentti (`f01a579`)
- Lisää toteutussuunnitelma ja sovelluskuvake (`04c7f87`)
- Lisää detekt CLAUDE.md:n build-tietoihin (`0b86ca9`)
- Merkitse consistency-seurantadokumentti valmiiksi (`bd3eabb`)
### Fixed
- Korjaa detekt-konfiguraatio 2.0-alpha-yhteensopivaksi (`757ea5f`)
- Lisää detekt 2.0.0-alpha.2 staattiseen koodianalyysiin (`c68502c`)
- Siirrä suomenkieliset privacy-merkkijonot oikeaan kohtaan (`91e0282`)
- Päivitä SpeedTestScreen käyttämään erillisiä tilafloita (`c62789e`)
- Päivitä NetworkDetailScreen käyttämään sealed-tilaa (`9f03548`)
- Muunna NetworkUiState sealed-rajapinnaksi ja erota speed test -tila (`fd800d9`)
- Apply redesign and consistency cleanup (`f1a5b73`)
- Implement crash reporting opt-in and tighten privacy (`88a47e0`)
### UI
- Refine home and battery UI (`9b95ba9`)
- Lisää kattava projektikuvaus suomeksi (PROJECT.md) (`084d8a1`)
- Synkronoi paikalliset muutokset ennen redesign-branchin pushia (`83c314b`)
- Siivoa käyttämättömät tiedostot, merkkijonot ja päivitä dokumentaatio (Vaihe 6) (`6fc53fb`)
- Yhtenäistä detaljinäkymien visuaalinen tyyli Avast-teemaan (Vaihe 5) (`d741082`)
- Uudista kotinäkymä ja navigaatiorakenne (Vaihe 3+4) (`2967222`)
- Lisää uudet jaetut komponentit ja päivitä olemassa olevat (Vaihe 2) (`52021a6`)
- Vaihda teema Avast-tyyliseen tummaan palettiin (Vaihe 1) (`d06741e`)
- Sync remaining local changes (`cce4a29`)
- Commit remaining local project changes (`21e5abf`)
- Refine health gauges and scoring UI (`9c77649`)
- Enforce Clean Architecture layering with domain interfaces and model mapping (`b2b29e3`)
- Add architecture layering review documenting Clean Architecture violations (`b4174c6`)
- Implement card-grid home screen per UI spec (`ff94e07`)
- Add files via upload (`dcc2dcf`)
- Add speed test feature to Network screen (Phase 1 MVP) (`f8c4d38`)
- Add files via upload (`b204bff`)
- Korjaa mittaukset ja teeman vaihto Android 17 betalla (`ff85ede`)
- Korjaa signaalin luokittelu 5G-taajuuksille ja latenssi (`7409663`)
- Korjaa verkkomittaukset ja storage-jaottelu (`da9db17`)
- Korjaa UI-näyttövirheet: enum-nimet, verkkotyyppi ja pisteytys (`e6ea59e`)
- Korjaa verkkotyyppi, storage-jaottelu ja dashboard-navigaatio (`e5be5ad`)
- Korjaa Android 17 beta SecurityException BatteryManager-kutsuissa (`908a1c7`)
- Korjaa Room-skeeman hash-ristiriita: versio 4→5 migraatio (`273c998`)
- Korjaa Dashboard/Battery-kaatuminen ja signaalivoimakkuuden näyttö (`98d0d5b`)
- Korjaa käynnistyscrash: SecurityException getDataNetworkType-kutsussa (`0a34cc2`)
- Lisää julkaisuvalmistelu: signing config, release checklist (Session P) (`6208139`)
- Lisää Firebase Crashlytics -integraatio beta-testausta varten (Session O) (`78c9b67`)
- Lisää Play Store -valmistelu: suomenkielinen lokalisointi, ProGuard, tietosuojakäytäntö (Session N) (`1aa97d3`)
- Lisää Phase 3 -ominaisuudet: laturivertailu, sovelluskohtainen akunkäyttö, throttling-loki, ilmoitukset ja CSV-vienti (`b45c32a`)
- Lisää kotinäyttöwidgetit Glance API:lla (Session I) (`75d8b7f`)
- Lisää laajennettu historia Pro-käyttäjille (Session H) (`f140ef0`)
- Lisää Google Play Billing -integraatio (Session G) (`65b6051`)
- Lisää AdMob-mainosintegraatio (Session F) (`266d219`)
- Lisää sparkline-kaaviot dashboard-kortteihin ja korjaa pull-to-refresh (`a580429`)
- Lisää yksikkötestit: battery normalizer, device profile, health score (`c6182c3`)
- Korjaa build-infrastruktuuri ja päivitä riippuvuudet uusimpiin (`13804d6`)
- Implement DevicePulse MVP — full Phase 1 Android app (`7238d7d`)
- Add detailed implementation plan for DevicePulse MVP (`75337c8`)
- Add files via upload (`914d3e5`)

